// Конфигурация эффектов мерцания для SVG
// Все параметры можно легко настроить здесь

window.SVGEffectsConfig = {
    // Определение мобильного устройства (можно настроить ширину экрана)
    mobileBreakpoint: 768,
    
    // Конфигурация для Desktop версии
    desktop: {
        // Базовое состояние (минимальное свечение)
        base: {
            shadows: [
                '0 0 1px rgba(255, 255, 255, 0.4)',
                '0 0 3px rgba(255, 255, 255, 0.3)',
                '0 0 5px rgba(255, 255, 255, 0.2)'
            ]
        },
        // Пиковое состояние (максимальное свечение)
        peak: {
            shadows: [
                '0 0 3px rgba(255, 255, 255, 0.6)',
                '0 0 6px rgba(255, 255, 255, 0.4)',
                '0 0 9px rgba(255, 255, 255, 0.3)',
                '0 0 14px rgba(255, 255, 255, 0.2)'
            ]
        },
        // Длительность одного цикла мерцания в миллисекундах (2500 = 2.5 секунды)
        duration: 2500,
        // Скорость перехода между состояниями (в секундах)
        transitionSpeed: 1.2
    },
    
    // Конфигурация для Mobile версии (такая же как на desktop)
    mobile: {
        // Базовое состояние (минимальное свечение)
        base: {
            shadows: [
                '0 0 1px rgba(255, 255, 255, 0.4)',
                '0 0 3px rgba(255, 255, 255, 0.3)',
                '0 0 5px rgba(255, 255, 255, 0.2)'
            ]
        },
        // Пиковое состояние (максимальное свечение)
        peak: {
            shadows: [
                '0 0 3px rgba(255, 255, 255, 0.6)',
                '0 0 6px rgba(255, 255, 255, 0.4)',
                '0 0 9px rgba(255, 255, 255, 0.3)',
                '0 0 14px rgba(255, 255, 255, 0.2)'
            ]
        },
        // Длительность одного цикла мерцания в миллисекундах (2500 = 2.5 секунды)
        duration: 2500,
        // Скорость перехода между состояниями (в секундах)
        transitionSpeed: 1.2
    }
};

